name: CI & Packaging

on:
  push:
    branches: [ "main" ]
    tags: [ "v*" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write
  actions: write

jobs:
  build-linux:
    name: Build Linux (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-24.04, ubuntu-22.04]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake build-essential libasound2-dev libgl1-mesa-dev libx11-dev libxrandr-dev libxinerama-dev libxcursor-dev libfreetype-dev libfontconfig1-dev

      - name: Configure CMake
        run: |
          cmake -B build \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_VERBOSE_MAKEFILE=ON

      - name: Build Jazz++ & Tests
        run: |
          cmake --build build --parallel $(nproc)

      - name: Run Unit Tests
        run: |
          ctest --test-dir build --output-on-failure

      - name: Verify Binaries
        run: |
          file build/jazz
          ls -lh build/jazz
          if [ -f "build/jazz-juce" ]; then
            file build/jazz-juce
            ls -lh build/jazz-juce
          fi

      - name: Upload Linux Binaries
        uses: actions/upload-artifact@v4
        with:
          name: jazz-linux-${{ matrix.os }}
          path: |
            build/jazz
            build/jazz-juce
          if-no-files-found: error
          overwrite: true

  appimage:
    name: Package AppImage (Ubuntu 22.04)
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake build-essential libasound2-dev file wget libgl1-mesa-dev libx11-dev libxrandr-dev libxinerama-dev libxcursor-dev libfreetype-dev libfontconfig1-dev

      - name: Build Project
        run: |
          cmake -B build -DCMAKE_BUILD_TYPE=Release
          cmake --build build --parallel $(nproc)
          ctest --test-dir build --output-on-failure

      - name: Download AppImage Tool
        run: |
          wget -q https://github.com/AppImage/appimagetool/releases/download/continuous/appimagetool-x86_64.AppImage -O appimagetool
          chmod +x appimagetool

      - name: Build AppDir and AppImage
        run: |
          ./scripts/build-appimage.sh build
          ARCH=x86_64 ./appimagetool --appimage-extract-and-run AppDir Jazz++-x86_64.AppImage
          ls -lh Jazz++-x86_64.AppImage

      - name: Upload AppImage Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Jazz++-x86_64.AppImage
          path: Jazz++-x86_64.AppImage
          if-no-files-found: error
          overwrite: true

  build-windows:
    name: Build Windows (MSVC x64)
    runs-on: windows-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Configure CMake
        shell: pwsh
        run: |
          cmake -B build -DCMAKE_BUILD_TYPE=Release

      - name: Build Jazz++ & Tests
        shell: pwsh
        run: |
          cmake --build build --config Release --parallel

      - name: Run Unit Tests
        shell: pwsh
        run: |
          ctest --test-dir build -C Release --output-on-failure

      - name: Package Windows Release (ZIP)
        shell: pwsh
        run: |
          $pkgDir = "Jazz++-Windows-x64"
          New-Item -ItemType Directory -Force -Path $pkgDir

          # Locate JUCE executable
          $exePath = Get-ChildItem -Path build -Filter "Jazz++.exe" -Recurse | Select-Object -First 1 -ExpandProperty FullName
          if (-not $exePath) {
            $exePath = Get-ChildItem -Path build -Filter "jazz.exe" -Recurse | Select-Object -First 1 -ExpandProperty FullName
          }
          if ($exePath) {
            Copy-Item $exePath "$pkgDir\jazz.exe"
            Copy-Item $exePath "$pkgDir\jazz-juce.exe"
          } else {
            Write-Error "Could not find built executable Jazz++.exe or jazz.exe"
            exit 1
          }

          # Copy assets and documentation
          Copy-Item -Recurse "conf" "$pkgDir\conf"
          Copy-Item "conf\jazz.cfg" "$pkgDir\" -Force
          Copy-Item -Recurse "song" "$pkgDir\song"
          Copy-Item -Recurse "src\HelpFiles" "$pkgDir\HelpFiles"
          Copy-Item "README.md", "COPYING", "ChangeLog" "$pkgDir\"

          # Compress to zip
          Compress-Archive -Path $pkgDir -DestinationPath "Jazz++-Windows-x64.zip" -Force

      - name: Upload Windows Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Jazz++-Windows-x64
          path: Jazz++-Windows-x64.zip
          if-no-files-found: error
          overwrite: true

  build-macos:
    name: Build macOS (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        os: [macos-14, macos-15]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Configure CMake
        run: |
          cmake -B build -DCMAKE_BUILD_TYPE=Release

      - name: Build Jazz++ & Tests
        run: |
          cmake --build build --parallel

      - name: Run Unit Tests
        run: |
          ctest --test-dir build --output-on-failure

      - name: Package macOS Release (ZIP)
        run: |
          mkdir -p "Jazz++-macOS"
          APP_PATH=$(find build -name "Jazz++.app" -type d | head -n 1)
          if [ -z "$APP_PATH" ]; then
            APP_PATH=$(find build -name "Jazz++ JUCE.app" -type d | head -n 1)
          fi
          if [ -n "$APP_PATH" ]; then
            cp -R "$APP_PATH" "Jazz++-macOS/"
          else
            echo "Error: Could not find Jazz++.app or Jazz++ JUCE.app" >&2
            exit 1
          fi
          cp -R conf song src/HelpFiles README.md COPYING "Jazz++-macOS/"
          zip -r "Jazz++-macOS-${{ matrix.os }}.zip" "Jazz++-macOS"

      - name: Upload macOS Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Jazz++-macOS-${{ matrix.os }}
          path: Jazz++-macOS-${{ matrix.os }}.zip
          if-no-files-found: error
          overwrite: true

  release:
    name: GitHub Release
    if: startsWith(github.ref, 'refs/tags/v')
    needs: [build-linux, appimage, build-windows, build-macos]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Download AppImage
        uses: actions/download-artifact@v4
        with:
          name: Jazz++-x86_64.AppImage

      - name: Download Windows Package
        uses: actions/download-artifact@v4
        with:
          name: Jazz++-Windows-x64

      - name: Download macOS Package (macOS 14)
        uses: actions/download-artifact@v4
        with:
          name: Jazz++-macOS-macos-14

      - name: Download macOS Package (macOS 15)
        uses: actions/download-artifact@v4
        with:
          name: Jazz++-macOS-macos-15

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            Jazz++-x86_64.AppImage
            Jazz++-Windows-x64.zip
            Jazz++-macOS-macos-14.zip
            Jazz++-macOS-macos-15.zip
          generate_release_notes: true
